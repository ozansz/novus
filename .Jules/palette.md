## 2024-05-22 - Accessibility Pattern for Clickable Elements **Learning:** This app relies heavily on `TouchableOpacity` which lacks semantic HTML mapping on web. `Pressable` with `accessibilityRole="button"` and `hovered` state handling is the preferred pattern for universal accessibility and interactivity. **Action:** Prefer `Pressable` over `TouchableOpacity` for all new interactive elements.
